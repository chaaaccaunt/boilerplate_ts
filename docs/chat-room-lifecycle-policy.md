# Политика жизненного цикла chat rooms

## Назначение

Документ описывает пользовательское удаление, выход из комнат и soft-archive закрытых комнат без административной очереди на удаление.

## Статусы комнат

`chat_rooms.status` описывает жизненный цикл комнаты:

- `active` — комната доступна обычным пользователям по правилам доступа;
- `archived_by_owner` — group room закрыта владельцем и больше не имеет пользовательского владельца;
- `orphaned` — private room больше не имеет активных участников и пользовательского владельца.

Обычные пользовательские списки комнат должны показывать только `active` rooms.

## Участники комнат

`chat_room_members.leftAt` фиксирует выход пользователя из комнаты.
Строка membership не удаляется физически при обычном выходе пользователя, чтобы сохранить историю участия.

Активным участником считается запись, у которой:

- `deletedAt` отсутствует;
- `leftAt` равен `NULL`.

Проверка доступа к непубличной комнате должна учитывать только активных участников.

## Public rooms

`public` room является системной обязательной комнатой.

Обычные пользователи не могут:

- удалять public room;
- покидать public room;
- переводить public room в архивный статус.

Отсутствие обязательной public room является controlled error, а не поводом выбирать fallback-комнату.

## Group rooms

`group` room имеет владельца.
В текущей модели владельцем считается `chat_rooms.createdByUserUid`, пока комната активна.
При создании чата пользователь выбирает участников, а backend сам определяет тип комнаты.
Если выбран один собеседник, создается `private` room.
Если вместе с владельцем в комнате больше двух участников, создается `group` room.
Название при создании не запрашивается: group room получает начальное техническое название и может быть переименована владельцем после создания.

Владелец group room может:

- изменить название комнаты;
- удалить комнату пользовательским действием.

Пользовательское удаление group room не выполняет hard delete.
При удалении владельцем:

- все активные участники получают `leftAt`;
- комната получает статус `archived_by_owner`;
- `createdByUserUid` очищается в `NULL`;
- `archivedAt` фиксирует время закрытия;
- сообщения и связи вложений остаются в базе как исторические данные;
- комната не попадает в административную очередь и не требует ручного удаления администратором.

Обычный участник group room, который не является владельцем, может покинуть комнату.
После выхода комната исчезает из его пользовательского списка, но остается доступной другим активным участникам.

## Private rooms

`private` room закрывается для каждого пользователя отдельно.

Если один участник выходит из private room:

- его membership получает `leftAt`;
- для второго активного участника комната остается доступной.

Если второй участник тоже выходит:

- комната получает статус `orphaned`;
- `createdByUserUid` очищается в `NULL`;
- `archivedAt` фиксирует время закрытия;
- комната скрывается из обычных пользовательских списков.
- комната не попадает в административную очередь и не требует ручного удаления администратором.

## Владелец закрытой комнаты

Закрытые комнаты не передаются техническому пользователю.
Для комнат со статусами `archived_by_owner` и `orphaned` поле `chat_rooms.createdByUserUid` должно быть `NULL`.
Пользовательский владелец существует только у активной `group` room.

## Soft-archive без административной очереди

Для boilerplate выполнен soft-archive без административной очереди.

Закрытые комнаты со статусами `archived_by_owner` и `orphaned`:

- не отображаются в обычных пользовательских списках комнат;
- не отображаются в отдельном admin dashboard;
- не требуют от администратора ручного hard delete;
- остаются в БД вместе с сообщениями, участниками и связями вложений.

Hard delete закрытых комнат, физическое удаление файлов и очистка связанных `stored_files` не являются пользовательским или административным UI-сценарием boilerplate.
Если конкретному проекту нужна политика очистки закрытых комнат, ее нужно реализовать отдельным явно описанным maintenance-сценарием с правилами хранения данных и безопасной проверкой повторного использования файлов.
